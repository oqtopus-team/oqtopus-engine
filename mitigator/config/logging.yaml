version: 1
formatters:
  logging_format:
    class: pythonjsonlogger.json.JsonFormatter
    format: "%(asctime)s.%(msecs)03d %(levelname)s %(filename)s:%(funcName)s:%(lineno)d %(message)s"
    datefmt: "%Y-%m-%dT%H:%M:%S"
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: logging_format
    stream: ext://sys.stdout
  file:
    class: logging.handlers.RotatingFileHandler
    level: DEBUG
    formatter: logging_format
    filename: "./logs/mitigator.log"
    maxBytes: 10485760 # 10MB
    mode: a # append
    backupCount: 9
    encoding: utf-8
  # OTel LoggingHandler — without this, dictConfig replaces the root
  # logger's handlers and silently drops the handler that
  # opentelemetry-instrument attached at startup, so OTLP log export
  # never happens.
  otlp:
    class: opentelemetry.sdk._logs.LoggingHandler
    level: DEBUG
loggers:
  oqtopus_engine_mitigator:
    level: DEBUG
    handlers: [console, file, otlp]
    propagate: False
root:
  level: WARNING
  handlers: [console, file, otlp]
disable_existing_loggers: False
